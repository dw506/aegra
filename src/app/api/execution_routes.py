"""Execution and approval route registration boundary."""

from __future__ import annotations

from typing import Any


def register_execution_routes(app: Any, orchestrator: Any, settings: Any) -> None:
    """Compatibility hook for execution/approval routes."""

    return None
